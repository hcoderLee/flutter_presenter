import 'package:flutter/material.dart';

class NetworkDemoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("NetworkDemo")),
      body: Column(
        children: <Widget>[],
      ),
    );
  }
}
