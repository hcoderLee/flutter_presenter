import 'package:flutter/material.dart';

class PlatformDemoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("PlatformDemo")),
      body: Column(
        children: <Widget>[],
      ),
    );
  }
}
