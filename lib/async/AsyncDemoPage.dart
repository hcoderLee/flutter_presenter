import 'package:flutter/material.dart';

class AsyncDemoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("AsyncDemo")),
      body: Column(
        children: <Widget>[],
      ),
    );
  }
}
